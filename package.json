{
  "name": "sneaker-heads",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd client && npm start\"",
    "build": "concurrently \"cd client && yarn build\" \"cd backend && yarn build\"",
    "start": "cd backend && node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dlittle1/sneaker-heads.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dlittle1/sneaker-heads/issues"
  },
  "homepage": "https://github.com/dlittle1/sneaker-heads#readme",
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "engines": {
    "node": "16.13.1",
    "npm": "8.1.2"
  }
}
